import { GameTactics, ActiveTactic } from './types';

export function processTacticalGrowth(stats: any, slots: string[]) {
  return { newStats: stats };
}

export function processTacticalDecay(stats: any, slots: string[]) {
  return { newStats: stats };
}
