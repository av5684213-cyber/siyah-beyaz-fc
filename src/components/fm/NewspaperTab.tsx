'use client';

import React from 'react';
import { ComingSoon } from './UIComponents';
import { Newspaper } from 'lucide-react';

export default function NewspaperTab() {
  return <ComingSoon title="Haberler" icon={<Newspaper size={40} />} />;
}
